query ListNotes {
    listNotes {
        id
        content
        title
        user {
            id
            username
            email
        }
        created_at
        updated_at
    }
}
