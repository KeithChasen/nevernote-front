query ListNotes($search: String) {
    listNotes(search: $search) {
        id
        content
        title
        user {
            id
            username
            email
        }
        created_at
        updated_at
    }
}
